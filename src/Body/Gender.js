

function Gender () {
    return (
        <div>
            <h1>Gender</h1>
            {/* {listShowGender.map((item, idx) => {
              return (
                <div key={idx}>
                  <input id={idx} type="checkbox" onChange={() => onChangeGender(item)} checked={item.isChecked} value={item.label} />
                  <label for={idx}>{item.label}</label><br/>
                </div>
              )
            })} */}
        </div>
    )
}
export default Gender;